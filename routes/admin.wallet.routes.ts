import { Router } from 'express'import { adminWalletController } from '../controllers/AdminWalletController.js'import { adminAuth } from '../middlewares/adminAuth.js'const router = Router()router.get('/admin/wallet/:id', adminAuth, (req, res, next) => adminWalletController.getUserWallet(req, res, next))router.get('/admin/wallet/:id/ledger', adminAuth, (req, res, next) => adminWalletController.getUserLedger(req, res, next))router.patch('/admin/wallet/:id/balance', adminAuth, (req, res, next) => adminWalletController.adjustBalance(req, res, next))export default router